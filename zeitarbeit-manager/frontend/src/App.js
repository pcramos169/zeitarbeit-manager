{/* Frontend setup with React */}

import React from 'react';
import ReactDOM from 'react-dom';

const App = () => (
  <div>
    <h1>Welcome to Zeitarbeit Manager</h1>
  </div>
);

ReactDOM.render(<App />, document.getElementById('root'));